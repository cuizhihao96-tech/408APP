---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 70ec127eeb367b93270d6b47feba2cfb_3669e437739e11f1aabe5254007bceed
    ReservedCode1: J624+6LGnSxiLBXD7SRsaK6H/LdqlpEJnErIcNYraO+6d9mlrtOHLZ58hmhWqHU7pfJ2ctJ6lAIU4487sJFN4oLAfCBtyPUfNzaaYVdx4HLsv+0PEi7HD+3nTzZ+KPauQBv+YFTx0lAPOkT1t18d72nIdr6gNrdce9RUqJ02u9z6qAgIT6nmSksyWjA=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 70ec127eeb367b93270d6b47feba2cfb_3669e437739e11f1aabe5254007bceed
    ReservedCode2: J624+6LGnSxiLBXD7SRsaK6H/LdqlpEJnErIcNYraO+6d9mlrtOHLZ58hmhWqHU7pfJ2ctJ6lAIU4487sJFN4oLAfCBtyPUfNzaaYVdx4HLsv+0PEi7HD+3nTzZ+KPauQBv+YFTx0lAPOkT1t18d72nIdr6gNrdce9RUqJ02u9z6qAgIT6nmSksyWjA=
---

# 408 刷题助手 v2 — 全面审查报告

> 审查时间：2026-06-29  
> 审查范围：`E:\dniit\Claude Code\program\408-app.2`  
> 数据文件：`questions.json`（1180 题，991 KB）

---

## 一、数据质量（P0/P1/P2）

### P0 — 阻塞级

| # | 问题 | 影响 | 详情 |
|---|------|------|------|
| 1 | **30 道选择题 answer 字段为文本而非整数索引** | 应用无法正确判分，这 30 题永远判错 | 全部集中在 `计算机组成原理`（co-*），answer 存的是正确选项的**文本**而非 `0/1/2/3` 索引。renderer.js 中 `handleChoiceAnswer` 用 `parseInt(opt.dataset.idx) === q.answer` 比较，永远为 false |
| 2 | **13 组完全重复题目（题干 100% 相同）** | 刷题时重复出现，统计失真 | 集中在 `计算机网络`（cn-*），如 `cn-134/cn-192`、`cn-142/cn-211` 等，共 13 对 26 题 |

### P1 — 重要

| # | 问题 | 影响 |
|---|------|------|
| 3 | **189 道大题 explanation 字段全部为空** | 大题无解析，用户做完无法核对思路 |
| 4 | **2 道选择题选项数异常**：`ds-239` 仅 2 个选项（answer=3 越界）、`os-284` 仅 3 个选项 | 应用渲染异常或崩溃 |
| 5 | **解析字段含教材污染内容** | 如 `os-277` 的 explanation 中混入了"#### 二、综合应用题"及甘特图描述文字，非纯解析 |
| 6 | **ID 不连续**（各科目缺失 10~25 个 ID） | 数据来源拼接痕迹明显，可能缺失题目 |

### P2 — 锦上添花

| # | 问题 |
|----|------|
| 7 | 部分题目题干含"【2016统考真题】"等来源标记，未统一清洗 |
| 8 | `toc.json` 未被应用使用（死数据），但体积达 140 KB |
| 9 | 无题目难度标注，无法做分层刷题 |

---

## 二、代码架构

### 整体结构

```
408-app.2/
├── main.js          (140 行)  Electron 主进程
├── preload.js       (11 行)   上下文桥接
├── package.json     (34 行)   项目配置
├── src/
│   ├── index.html   (107 行)  入口 HTML
│   ├── renderer.js  (786 行)  所有渲染逻辑（单体）
│   └── style.css   (502 行)  样式
└── data/
    ├── questions.json  (991 KB, 1180 题)
    ├── temp_bookmarks.json (93 KB, 未使用)
    └── toc.json (140 KB, 未使用)
```

### P0 — 阻塞级

| # | 问题 | 详情 |
|---|------|------|
| 1 | **`main.js` 置顶实现为 PowerShell 定时调用 `SetWindowPos`** | 每 5 秒执行一次 `powershell -Command ...`，属于侵入式 Hack；用户无法永久关闭置顶（UI 开关只控制 Electron 层，PowerShell 定时任务仍在运行） |
| 2 | **`renderer.js` 786 行单体文件，无模块化拆分** | 所有功能（首页/答题/错题/统计/导入导出）全在一个文件，维护成本极高 |

### P1 — 重要

| # | 问题 | 详情 |
|---|------|------|
| 3 | **`main.js` 中 `load-questions` 使用 `readFileSync` 同步读取 1MB JSON** | 阻塞主进程，启动卡顿；应改为异步 `readFile` 或流式读取 |
| 4 | **`localStorage` 无版本管理** | 数据结构变更（如新增字段）会导致旧数据解析异常，无迁移机制 |
| 5 | **preload.js 暴露的 API 过少** | `loadQuestions` 每次都从磁盘重读；应支持增量更新或内存缓存 |
| 6 | **无错误处理边界** | `renderer.js` 中任何未捕获异常（如题目数据格式错误）会导致整个应用白屏 |

### P2 — 锦上添花

| # | 问题 |
|----|------|
| 7 | 未使用 TypeScript，无类型保护 |
| 8 | 无 ESLint / Prettier 配置 |
| 9 | `temp_bookmarks.json` 和 `toc.json` 未被任何代码引用，属于冗余数据 |

---

## 三、交互体验

### 已实现功能

- 科目/章节两级选择 ✅
- 随机刷题（洗牌）✅
- 选择题自动判分 + 解析展示 ✅
- 大题自判（对了/错了）✅
- 错题本（按科目/章节筛选）✅
- 收藏功能 ✅
- 已学过记录 ✅
- 学习统计面板（科目进度 + 7 日趋势图）✅
- 数据导入/导出 ✅
- 键盘快捷键（A/B/C/D、Enter、Esc）✅
- 搜索（题干/章节/选项子串匹配）✅

### P1 — 重要缺失

| # | 缺失功能 | 说明 |
|---|----------|------|
| 1 | **计时/限时模式** | 无法模拟真实考试的时间压力 |
| 2 | **模拟考试模式** | 无"一套卷子"概念，无法按试卷维度刷题 |
| 3 | **笔记/标注** | 用户无法在题目上添加个人笔记 |
| 4 | **题目反馈/报错** | 遇到错题（数据错误）无法反馈给开发者 |

### P2 — 锦上添花

| # | 缺失功能 |
|----|----------|
| 5 | 搜索不支持拼音/模糊匹配 |
| 6 | 无撤销功能（答完一题不可撤回） |
| 7 | 无题目收藏夹分类（当前只有单一收藏列表） |
| 8 | 统计面板无导出功能（无法生成学习报告 PDF） |
| 9 | 无暗色/亮色主题切换（当前固定暗色） |

---

## 四、UI / 视觉

### P1 — 重要

| # | 问题 | 详情 |
|---|------|------|
| 1 | **无亮色主题** | 固定暗色，强光环境下可读性差 |
| 2 | **窗口最小宽度 420px，无响应式适配** | 无法在窄屏（如手机端）使用；`dist` 中虽有 `408刷题助手-手机版.zip` 但源码中无对应实现 |
| 3 | **无 Loading / Skeleton 状态** | 启动时 `loadQuestions` 读取 1MB 数据期间界面空白 |

### P2 — 锦上添花

| # | 问题 |
|----|------|
| 4 | 科目图标使用 Unicode 字符（🖥️🌲🔧🌐），不同平台渲染不一致 |
| 5 | 无视图切换动画，体验生硬 |
| 6 | 统计面板柱状图无数字标注，只有视觉高度 |

---

## 五、功能完整性（与同类产品对比）

| 功能 | 本题库 App | 主流竞品（Anki/墨墨/粉笔） |
|------|------------|--------------------------|
| 选择题刷题 | ✅ | ✅ |
| 大题刷题 | ✅（无解析） | ✅ |
| 错题本 | ✅ | ✅ |
| 收藏 | ✅ | ✅ |
| 学习统计 | ✅（基础） | ✅（详细） |
| **间隔重复（SRS）** | ❌ | ✅（核心功能） |
| **模拟考试** | ❌ | ✅ |
| **计时模式** | ❌ | ✅ |
| **笔记/标注** | ❌ | ✅ |
| **数据云同步** | ❌ | ✅ |
| **题目朗读（TTS）** | ❌ | ✅（部分） |
| **导出 PDF/Anki** | ❌（仅 JSON） | ✅ |
| **题目难度标注** | ❌ | ✅ |

### P1 — 高价值缺失功能

1. **间隔重复算法**（如 SM-2）—— 这是刷题 App 的核心价值
2. **模拟考试模式** —— 按 408 真题试卷结构出题
3. **题目笔记** —— 用户可添加个人理解

---

## 六、工程化

### P1 — 重要

| # | 问题 | 详情 |
|---|------|------|
| 1 | **无 ESLint / Prettier** | 代码风格不统一，潜在 bug 难以发现 |
| 2 | **无测试** | 零单元测试/集成测试，数据格式变更极易引入回归 |
| 3 | **`.gitignore` 不完整** | 缺少 `output/`、`*.exe`、`*.pak` 等；`dist/` 虽已忽略但 `dist` 目录下有大量文件被误打包进 `git` |
| 4 | **`package.json` 无 `repository`/`author`/`license` 字段** | 不符合 npm 规范 |

### P2 — 锦上添花

| # | 问题 |
|----|------|
| 5 | 未使用 TypeScript |
| 6 | 无 CI/CD 配置（如 GitHub Actions 自动打包发布） |
| 7 | `electron-builder` 配置中 `files` 包含 `node_modules/**/*`，导致打包体积膨胀（dist exe 达 226 MB） |
| 8 | 无 CHANGELOG，版本管理粗糙 |

---

## 七、问题汇总与优先级

### P0（必须修复，阻塞发布）

1. **修复 30 道计组选择题的 answer 字段**（将文本答案转为整数索引 0~3）
2. **清除 13 组重复题目**（删除重复 ID，保留其一）
3. **修复 `ds-239` 选项缺失问题**（补充缺失选项或合并题目）

### P1（重要，应优先处理）

4. 补充 189 道大题的 explanation 字段
5. 修复 `main.js` 置顶 PowerShell Hack，改用纯 Electron API
6. 将 `renderer.js` 拆分为模块（`home.js` / `quiz.js` / `review.js` / `stats.js`）
7. 增加计时/模拟考试功能
8. 增加笔记功能
9. 增加间隔重复（SRS）算法
10. 补充 ESLint + 基础测试用例

### P2（体验优化，有空再做）

11. 增加亮色主题
12. 增加拼音/模糊搜索
13. 统计面板增加导出 PDF
14. 优化打包体积（精简 `node_modules` 打包范围）
15. 增加题目难度标注和分层刷题

---

## 八、修复建议（针对 P0）

### 修复 answer 字段（30 道计组题）

```python
# 修复脚本思路：
# 这30道题的 answer 字段存的是正确选项的文本
# 只需找到该文本在 options 数组中的索引，替换为整数
for q in questions:
    if q['type'] == 'choice' and not isinstance(q['answer'], int):
        q['answer'] = q['options'].index(q['answer'])
```

### 清除重复题目

```python
# 按题干去重，保留 ID 较小的那条
seen = {}
for q in questions:
    key = q['question']
    if key in seen:
        print(f"重复: {seen[key]} vs {q['id']}")
    else:
        seen[key] = q['id']
```

---

*报告结束*
*（内容由AI生成，仅供参考）*
